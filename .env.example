# Frontend override. Leave empty in normal Vercel deployment so the app uses /api/read-cloud.
VITE_ORACLE_API_URL=

# Server-side custom oracle endpoint. This can point to your own OpenAI, Anthropic,
# Replicate, Worker, or self-hosted API adapter.
ORACLE_API_URL=
ORACLE_API_KEY=
